spring.application.name=CampusEats
# Database Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/campuseats?useSSL=false&serverTimezone=UTC
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate properties
# This automatically creates/updates tables in MySQL based on your Java code
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# View naming
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

app.admin.default-password=SuperSecureAdminPass!2026
razorpay.key.id=rzp_test_SpxU56X5cpDvBX
razorpay.key.secret=qQ1366Vu2qxaIK6UGbpw3Swe

# Increase maximum upload size to 10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Increase Tomcat's default multipart limit from 10 to 100
server.tomcat.max-part-count=100